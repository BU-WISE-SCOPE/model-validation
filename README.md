# Model Validation

This repository contains code for validating different ASR models on our datasets. Mainly used to evaluate performance of detecting individual phonemes and words in the audio data. The main script is `run_validation.ipynb` which runs the validation pipeline for different ASR models.
